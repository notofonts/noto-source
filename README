This is a branch for storing build artifacts, to review changes to the sources.

When changes are pushed to a staging branch, a continuous integration framework
should build the new fonts, compare with old fonts, and put the results of that
comparison here for review.
